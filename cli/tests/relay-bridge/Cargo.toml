[package]
name = "llmgate-relay-probe"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
anyhow = "1"
relaygate-sdk = "=0.5.1"
tokio = { version = "1", features = ["full"] }
tokio-util = { version = "0.7", features = ["rt"] }
