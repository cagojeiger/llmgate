global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: llmgate
    metrics_path: /metrics
    static_configs:
      - targets:
          - llmgate:8080

  - job_name: prometheus
    static_configs:
      - targets:
          - prometheus:9090
