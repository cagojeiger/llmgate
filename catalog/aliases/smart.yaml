# smart — 검토 / 판단 / 분석 (리즈닝). 설계 판단 / 복잡한 요구사항 분해 /
# 실패 원인 분석 / 코드 리뷰 / 답변 검증. 기준: 추론 능력·정확도·복잡 문제 해결.
# 권장:  context 256K  /  output 128K       (호출 측 등록 권장값)
# 상한:  context 1M     /  output 131,072    (체인 min = 실제 천장)
# 이미지: ✔ 전원 지원 → 다이어그램/스크린샷 기반 판단·분석 가능.
#   qwen3.8-max context 1,000,000 / output 131,072 / image ✔ (text,image,video) — 범용추론·분석 최강 주력
#   minimax-m3  context 1,000,000 / output 131,072 / image ✔ (text,image,video) — 코딩·에이전트까지 커버하는 만능 폴백
# (OpenCode Go 전용, 한도 출처: models.dev provider=opencode-go)
alias: smart
chain:
  - qwen3.8-max
  - minimax-m3
